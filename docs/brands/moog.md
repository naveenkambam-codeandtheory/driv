# Onboarding: Moog

Key `moog` · repoless (own site, `pathPrefix: "/"`) · locales en-us
Generated by `scripts/onboard-brand.mjs`, then corrected by hand: the script defaults to
path-prefix shape and doesn't know this project is repoless — see brands.json's real
`pathPrefix`/`hosts` and the CI workflow's `include:` entry, both already fixed.
Audit progress with:

```bash
node scripts/onboard-brand.mjs moog --check
```

## Automated

- [x] Registry entry in `brands.json` (corrected to repoless: `pathPrefix: "/"`, real hosts)
- [x] `scripts/brands.js` (the JS companion module `brand.js` actually imports — the
      onboarding script doesn't touch this file; had to be added by hand, and is now the
      thing `validate-tokens.mjs` checks stays in sync going forward)
- [x] Token file `styles/brands/moog.css` — all 57 values filled from real data extracted
      from moogparts.com's served CSS (colours, fonts, spacing, radii, shadows, durations
      all frequency-ranked from actual usage, not guessed); `node scripts/validate-tokens.mjs`
      passes
- [x] `icons/moog/favicon.ico` — real, downloaded from moogparts.com
- [x] Content index — `indexPath: /query-index.json`, defined in the Configuration Service
      for moog's own site, not a `/moog/**` path glob
- [x] Per-brand CI matrix entry (`include: preview_host: moog--naveenkambam-codeandtheory`,
      added by hand — the script only appends to the brand list, not `include:`)

## Design and engineering

- [ ] `icons/moog/logo.svg` and `logo-inverse.svg` — the live site's logo is only available
      as a PNG; design needs to supply proper SVG exports, not a raster conversion
- [ ] `fonts/moog/*.woff2` — moogparts.com uses `blenderpro-bold` (headings) and
      `roboto-regular` (body); these are licensed webfonts, not something to scrape from
      the live site — source properly licensed files for the same families
- [ ] Every value in `styles/brands/moog.css` reviewed and confirmed by the design team —
      "extracted from the live site" establishes what Moog looks like today, not that it's
      what this project should ship; e.g. the heading scale is real but flat (no evidence
      of a mobile/desktop split), which may or may not be the intended experience here
- [ ] Contrast verified against **this brand's** tokens (4.5:1 body, 3:1 large and UI)
- [ ] Authoring filters scoped

## Content

- [ ] Moog's site content created at its own root (`nav`, `footer`, `placeholders.json`,
      `metadata`) — there's no `/moog/` path under driv's site; it's a separate site
      sharing this code repository
- [ ] `theme: moog` set in bulk metadata (still worth doing even though hostname resolution
      already identifies this site — metadata resolves first and costs nothing extra)
- [ ] Placeholder keys copied from an existing brand so none render a fallback
- [ ] Template page per page type
- [ ] Sitemap and launch redirects
- [ ] Content source and index actually configured for the moog site in
      `tools.aem.live` (confirm hostnames above match what's registered there)

## Gates

- [ ] `node scripts/validate-tokens.mjs`
- [ ] `npm run lint`
- [ ] Lighthouse mobile 100/100/100/100 on this brand's preview URL
- [ ] axe clean on home, a listing page, a detail page
- [ ] Smoke page exercising every shared block, visually checked
- [ ] Existing brands re-verified as unaffected

## Sign-off

- [ ] **Zero shared JavaScript modified.** If any step required it, log it as a framework
      defect: the fix is to lift that difference into a token, a capability flag or
      content, not to add a branch. Every branch added here is a tax the next brand pays.
- [ ] Onboarding time recorded and friction points logged
